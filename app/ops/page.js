import { OperationsPage } from '../page';

// Shared Operations surface. The implementation stays exported from app/page.js so existing
// imports remain stable while the public URL becomes the explicit `/ops` tab.
export default OperationsPage;
