# Group 4.5 — Production vs Demo Data Inventory (+ demo-data plan)

**Purpose:** track exactly which data in the live DB is **real/production** (must never be fabricated
over or lost) vs **demo/seed** (safe to reset), so fake data for a demo can be layered on **around**
the real masters without corrupting them. Sits between Group 4 and Group 5.

**As of 2026-08-17**, derived by querying the live Turso dev DB directly (`.env.local`'s `TURSO_URL`
— the shared cloud DB, not the 0-byte local file). Re-run the inventory before a demo if time has
passed; the counts below are a point-in-time snapshot. **This revision supersedes the 2026-08-04
snapshot below it — the demo project set was fully wiped and rebuilt on this date** (see "2026-08-17
rebuild" below).

---

## The one golden rule

> **`suppliers` (445 rows) is real client data. Everything else is demo/seed, an accumulating
> master, or empty.** Never re-fabricate suppliers, and remember any *supplier master re-import
> wipes `supplier_quotes` + `purchase_orders`** (see "Wipe gotcha" below).

---

## 2026-08-17 rebuild — what changed and why

All per-project/demo data (the old `projects` 3/4/5/6/SB-1018 set, their BOM, QC, packing, tasks,
notifications) was deliberately wiped and rebuilt as `scripts/seed-demo-pipeline.mjs`, confirmed
with the user first (all of it was test data, nothing real). The goal was 3 projects that each sit
at one clear, distinct pipeline stage — Lead through Sale Order with no project yet, mid-build with
Design in progress, and near-complete — instead of a set of demo projects that all looked roughly
the same. Full design context: session notes and `PRODUCTION-MODULE-DESIGN.md`.

**Left untouched by the wipe** (config/masters, same treatment as `suppliers`): `users`, `employees`,
`operations`, `workstations`, `trades`, `stage_templates`/`stage_template_items`,
`calc_formulas`/`calc_formula_versions`/`calc_formula_tests`/`calc_tables`/`calc_table_rows`/
`calc_validations`/`calc_templates` (global engineering methodology, not project data), `suppliers`,
`counters`, `holidays`, `system_migrations`.

**Also left untouched, but reclassified below:** `customers`. The 2026-08-04 snapshot called this
"MASTER READY, NOT LOADED" at 0 rows. It is no longer empty — Sales/CRM shipped since then and the
table now behaves as an accumulating master, the same pattern as `suppliers`: the new seed script
never deletes it, and reuses an existing row by name (`getOrCreateCustomer`) instead of erroring on
the `UNIQUE(name)` constraint. It currently holds 3 rows, all created by the rebuild.

---

## 2026-08-19 — Stores gate-feature seed (STERP items 9/14/15, SYSTEM.md §5e)

New `scripts/seed-stores-gate-demo.mjs`, scoped to exactly the three new tables the round added:
`gate_inward_receipts`, `gate_passes`, `gate_pass_items`. Re-runnable — wipes its own scope first,
same contract as `seed-demo-pipeline.mjs`. Does not touch `inventory_items` (Reorder Suggestions
needed no new seed — MS Angle 50x50x5, already in the DB at 8 on-hand / 10 minimum, already
triggers it) or any project/BOM table, so it's safe to run regardless of which pipeline-demo state
is currently loaded.

Seeded: 2 GIRs (one open awaiting a GRN, one closed with `GRN-2031`) and 6 Gate Passes covering
every status the UI exercises — draft, approved, issued-not-yet-due, issued-**overdue** (the one
row that actually shows the Overdue badge), fully returned, and non-returnable-issued. Party/
supplier names are drawn from the real `suppliers` master (e.g. "Tools & Spares Corporation",
"JR SEAMLESS PVT LTD") rather than invented companies, matching the golden rule's spirit even though
these free-text fields aren't FK-linked to `suppliers`.

Run: `node --env-file=.env.local scripts/seed-stores-gate-demo.mjs`.

---

## 2026-08-23 — Dispatch accounting-integration demo row (SYSTEM.md §5aj)

Not a seed script — one manually-created row, left in place for demo purposes per instruction
(the same "safe to ignore" precedent as the Fixed Assets test asset, §5z/§5ac).

**`packing_lists` id 11, `PL-1009`**, `customer_name` = `"DISPATCH-DEMO-EXAMPLE (safe to ignore)"`,
under real project 17 (Shanti Boilers). Exercises every new Dispatch accounting field at once:
`sales_invoice_id` linked to the real invoice `SB/13/2026-27` (id 15), `freight_amount` 4,500,
`freight_paid_by` = `'us'`, `eway_bill_no` `EWB-DEMO-123456`, `status` = `'dispatched'`
(`dispatched_at` stamped). The freight expense has been posted to the ledger — real
`journal_entries` row (`source_type = 'dispatch_freight'`, `source_id = 11`), Dr Freight Expense
(5500) / Cr Bank & Cash (1001), ₹4,500 — visible in Shanti Boilers' real Trial Balance and on the
new Dispatch Register report.

**Not a production shipment** — delete `packing_lists` id 11 (cascades to `packing_items`, none
exist) and the linked `journal_entries`/`journal_entry_lines` (id 20) together if this needs
cleaning up later; deleting only one side would leave the other orphaned. A disposable twin of this
same setup (created, verified, then fully deleted — Trial Balance confirmed back to exact baseline
afterward) is how the feature was live-verified in the first place; this one row was deliberately
left rather than deleted.

---

## 2026-08-21 — Manufacturing lifecycle lineup rebuild + SO→Project conversion feature + Sales/Marketing inventory (this session)

**Superseding update — the 08-17/08-19 counts below for `projects`, `sale_orders`, `quotations`,
`leads`, `opportunities`, `customers`, `bom_items`, `job_cards`, `qc_records`, `packing_lists`,
`milestones`, and `notifications` are stale.** Queried live against Turso just now; re-run before
trusting any of those specific rows further. Everything else in the table (suppliers, items,
employees, gate passes, etc.) was not touched this session and should still be accurate as of its
own last-dated note.

**What changed, in order:**
1. **QC notification code** (not data) — `bom_items` reaching `Received` now notifies QC (first
   receipt) and Production+QC (fully received); a `qc_records` result flipping to `fail` notifies
   Procurement or Production. See `app/api/bom-items/[id]/route.js` and
   `app/api/qc-records/[id]/route.js`.
2. **Projects went from the 08-17 rebuild's 3-project set to a 10-project manufacturing-lifecycle
   lineup**, each project deliberately repositioned (via direct SQL, not the app) to sit at one
   clear stage — Design → Procurement → Material Receipt → Production → QC (pass and fail branches)
   → Dispatch → Installation — using the **existing** SB-1018/SB-1023…SB-1032/RM-DEMO-1 project
   numbers (kept as-is, not renamed). Full per-project detail (what's KEEP/MODIFY/ADD and why) lives
   in this session's transcript, not restated here — ask if it needs to be captured as its own doc.
   `RM-DEMO-1` and `SB-1031` were deliberately left outside the curated lineup (SB-1031 unused/spare).
3. **`ConvertSaleOrderButton.jsx`** (new) — Design head's own Sale-Order-to-Project entry point on
   `/projects`, fixing a real reachability gap (`/sales` is gated on Sales/Marketing department
   access, which a Design-only head never has, even though the convert action itself is gated on
   `isDesignHead`). The old Sales-side `ConvertToProjectDialog` in `SalesWorkspace.jsx` was removed
   as redundant once this shipped — verified live both ways (design_head reaches it on /projects;
   admin's Sales workspace no longer shows a Convert button, Costing/Request-from-Stores unaffected).
4. **New customer + quotation + Sale Order + Project**, seeded specifically to exercise #3 live:
   customer **Bharat Sugar Mills Ltd** (id 14), quotation **QTN-23/SB/2026-27** (accepted, 2 items,
   ₹47.79L), Sale Order **SO-20** (2 `sale_order_items`), converted live through the real UI by
   `design_head` into **SB-1039** (project id 36) — Scope of Supply auto-populated from the SO,
   correct `sos_created`/`project_created` notifications fired. **Deliberately kept, sits outside the
   curated 10-project lineup** — it's the "fresh SO→Project conversion" example, not part of that
   story.
5. **`lib/data.js`'s `getSaleOrders()`** gained an additive `item_count` column (a subquery against
   `sale_order_items`) — the real "ready to become a Project" signal, since only a Quotation→convert
   SO ever gets real line items; the quick-add "New Sale Order" dialog never did and still doesn't.
   Used to filter the Design-head picker; no new status/button was added to Sales (checked first —
   the Quotation-convert flow already notifies Design+PMs at the right moment, see
   STORES-SALES-CHANGES.md §2b).

**Sales + Marketing read-only inventory (queried 2026-08-21, before any Sales/Marketing seeding —
none has happened yet as of this note):**

| Table | Rows | Notes |
|---|---:|---|
| leads | 11 | **All 11 are `status='converted'`, all `source='referral'`, all `owner_dept='Sales'`.** Nothing sits in new/contacted/qualified/lost — the funnel's working states have zero examples. None have a `campaign_id` set. |
| customers | 12 | 11 richly detailed (GST/phone/email/city/state) from the 08-17 rebuild + Bharat Sugar Mills Ltd (id 14, thin — city/state only, no GST/phone/email, from item 4 above). |
| opportunities | 5 | 3 Sales-owned (`Asian Brown — 8T boiler upgrade` Quoted ₹32L, `HKM Charitable — AMC renewal` Qualified ₹8.5L, `Virchow Biotech — steam line expansion` Lead ₹54L) + 2 Marketing-owned (`Trade fair 2026` Lead ₹12L, `Regional distributor tie-up` Won ₹21L). **Zero Lost.** `opportunity_items` is 0 rows (always has been — no line-item detail ever gets attached to an opportunity in this app). |
| quotations / quotation_items | 13 / 15 | 12 `accepted` (11 from the 08-17 rebuild + QTN-23 from item 4) + 1 `draft` (QTN-22, ₹3,776 — looks like a stray small test row, not a real quote). |
| sale_orders / sale_order_items | 12 / 14 | SO-9 through SO-19 (11, all already converted to a project) + SO-20 (item 4, now also converted, to SB-1039). All `status='open'` — none ever marked `fulfilled`/`cancelled`. |
| campaigns | **0** | Completely empty — Marketing's one genuinely own-branded tab (vs. Leads/Enquiry which it shares with Sales) has nothing to show. |
| crm_notes | **0** | Completely empty. Also silently breaks the Agent Performance report's "average response time" metric (SYSTEM.md §5e) — that metric is computed from a lead's first `crm_notes` row, which doesn't exist for any lead. |
| price_lists | 1 | One default rate only (MS Plate 10mm @ ₹3,200/Mtr, `customer_id=NULL`). No customer-specific override exists to demo the "customer rate wins over default" precedence rule. |
| sales_returns | 1 | One, already `accepted`/`returned_to_stock` (the SYSTEM.md §5e live-verification artifact — restocked 5→8 on an inventory item). Nothing sits `pending` to demo the inspect step live. |
| sales_invoices / sales_invoice_items | 1 / 1 | One, `status='paid'`, ₹41.3L, against SO-17 (Narmada Chemicals). Nothing in `draft`/`issued` to demo the create→issue transition live. |
| sales_credit_notes / items | 1 / 1 | One, `status='issued'`, ₹1L, against that same invoice. |
| sales_stages | 5 | Config, not demo data — Lead/Qualified/Quoted/Won/Lost pipeline columns, correctly seeded, matches `opportunities.stage` 1:1. |
| crm_assignment_rules | **0** | Round-robin lead auto-assignment is unconfigured — the Team tab's own tagline ("Auto-assign new leads round-robin") has nothing to actually demonstrate. |
| crm_saved_views | 0 | No saved filter views. Low-stakes, user-preference feature. |
| contacts | **0** | Per-customer named-contact entity (Phase 2 "Selling") — entirely unused across all 12 customers. |
| Sales/Marketing `users` | 4 | `sales_head`, `marketing_head`, `sales`, `market` — one head + one member per department. Adequate as an assignment-target pool. |

**Key workflows and whether they're currently demoable end-to-end:** Lead→qualify→convert
(**blocked**, no lead sits in a pre-converted state), Opportunity stage progression (**partial**, 3
Sales-side opportunities span 3 different stages but none reach Won on a live click-through),
Quotation draft→accept (**demoable**, QTN-22 sits in draft), SO→Project conversion (**proven live**,
item 4 above), Sales Costing (**untested this pass** — needs a project with an issued PO and logged
labor time to show non-zero numbers, not yet checked which of the 13 active projects qualify),
Sales Returns raise→inspect→accept (**blocked**, the one example is already fully resolved), Invoice
draft→issue (**blocked**, the one example is already paid), Campaign→Lead attribution (**blocked**,
0 campaigns and every lead has `campaign_id=NULL`), Price List precedence (**thin**, only the
default rate exists), Agent Performance report (**renders, but response-time column will be blank
for every lead** — depends on `crm_notes`, which is empty), Team round-robin auto-assign
(**blocked**, `crm_assignment_rules` is empty).

**Also updated in passing** (touched by items 1-4 above, not a Sales/Marketing change but affects
the table below): `projects` 3→**13 active** (+1 sentinel = 14 total), `milestones` 50→**302**,
`bom_items` 9→**49**, `job_cards` 2→**4**, `qc_records` 0→**5**, `packing_lists`/`packing_items`
1/5→**2/8**, `notifications` 5→**93** (mostly from the 08-17→08-21 milestone/BOM/handoff activity
across the larger project set, not a red flag). `supplier_quotes`/`purchase_orders`/`po_items`/
`procurement_requests` moved from the 08-19 gap's reported 0 to **2/1/1/1** — still very thin, Gap 1
below is improved but not resolved; not re-verified in depth this pass.

---

## 2026-08-22 — Sales + Marketing demo pipeline seeded (coherent funnel, additive)

**New file: `scripts/seed-sales-marketing-demo.mjs`.** Run: `node --env-file=.env.local
scripts/seed-sales-marketing-demo.mjs`. Additive and re-runnable, same precedent as
`scripts/seed-stores-gate-demo.mjs` — **never** touches `scripts/seed-demo-pipeline.mjs`'s scope
(that script is a full destructive wipe of projects/BOM/QC/etc; running it would destroy the
10-project manufacturing lineup + SB-1039). Deletes only its own previously-seeded rows first
(marked `created_by='sm-demo-seed'`; `contacts` has no `created_by` column, marked via
`notes='sm-demo-seed'` instead), then re-inserts. **Verified idempotent — run 4 times in a row,
final counts identical to the 2nd run, `PRAGMA foreign_key_check` clean (0 violations) throughout.**
Two re-run bugs were found and fixed during that verification, both now handled by the script
itself: (1) `sale_order_no`/`quotation_no` counters must be repaired **after** this script's own
inserts too, not just before — fixed by calling the repair function a second time at the end of
`main()`; (2) `opportunity_items` on the pre-existing "Asian Brown — 8T boiler upgrade" opportunity
has no `created_by` column and that parent opportunity isn't marked either, so nothing wiped its 3
items between runs — they'd triple every re-run. Fixed with an explicit
`DELETE FROM opportunity_items WHERE opportunity_id = <that id>` right before re-inserting them.

**Framing:** reuses the existing 12 customers as **repeat/expansion business** (explicit decision —
no new fictional prospects). The 11 pre-existing converted leads, their customers, and the 12
pre-existing quotations/sale-orders/projects are **untouched** — confirmed via
`WHERE created_by IS NULL OR created_by != 'sm-demo-seed'` still returning exactly 11 leads, and
`customers` still exactly 12 rows after seeding.

**What was added (all figures are the steady-state count after the fix, i.e. what's in the DB now):**

| Table | Before → After | What's new |
|---|---|---|
| `campaigns` | 0 → **3** | Boiler Expo 2026 (completed, trade_show), Sugar & Distillery Email Outreach (active), Website Enquiry Forms (active). The pre-existing "Trade fair 2026" opportunity's `campaign_id` backfilled to Boiler Expo. |
| `leads` | 11 → **16** | 5 new, reusing existing customers: Godavari (`new`), Asian Brown (`contacted`), Himalayan (`qualified`), Narmada (`new`), Malwa Steel (`lost`). Full funnel spread, `assigned_to` set, each with a `campaign_id`. |
| `crm_notes` | 0 → **5** | Call/email/meeting activity on the contacted/qualified/lost leads, dated after each lead's `created_at` — feeds the Agent Performance report's response-time metric, previously always blank. |
| `opportunities` / `opportunity_items` | 5/0 → **6/3** | Backfilled `customer_id` on the pre-existing Asian Brown opp + itemized it (3 lines). Added the one missing stage: "Malwa Steel — 3rd boiler" (`Lost`, tied to the lost lead). |
| `quotations` / `quotation_items` | 13/15 → **16/20** | QTN-24 (`draft`, Asian Brown, linked to its opportunity), QTN-25 (`sent`, Godavari repeat order), QTN-26 (`accepted`, Himalayan AMC — feeds the new SO). |
| `sale_orders` / `sale_order_items` | 12/14 → **13/16** | **SO-21** (Himalayan, `open`, unconverted) — appears in the Design-head "Convert Sale Order" picker built two sessions ago. |
| `sales_returns` | 1 → **2** | New: `pending` return against SO-17 (Narmada/SB-1029) — demoable inspect→accept→restock; the original row was already resolved. |
| `sales_invoices` / items | 1/1 → **3/3** | New `draft` (Asian Brown, SO-11/SB-1018) + new `issued`-unpaid (Himalayan, SO-19/SB-1031, `project_id` set). Both demoable draft→issue→pay. |
| `sales_credit_notes` / items | 1/1 → **2/2** | New `draft` credit note against the new issued invoice. |
| `contacts` | 0 → **5** | One primary named contact each on Asian Brown, Godavari, Himalayan, Narmada, Bharat Sugar Mills. |
| `crm_assignment_rules` | 0 → **2** | Sales `["sales_head","sales"]`, Marketing `["marketing_head","market"]` — round-robin now configured. |
| `price_lists` | 1 → **4** | +1 customer-specific override (Asian Brown, MS Plate 10mm @ ₹3,000 vs. the ₹3,200 default — demos precedence) + 2 more default rates. |

**Counter drift — both instances now fixed, not just noted.** `sale_order_no` was `19` while `SO-20`
already existed (from the prior session's live SO→Project conversion demo) — the very next
app-generated SO would have collided. `quotation_no` was already safe. Both counters are now
verified ≥ the true max of every existing number (`sale_order_no=21`, `quotation_no=26` — matching
the new SO-21/QTN-26 this seed added) via `MAX(value, excluded.value)` upserts that re-run safely.

**Browser-verified** (as admin, `/sales` → Leads): the 5 new leads render correctly with working
`Convert` buttons on the new/qualified rows; the 11 historical `converted` leads are unchanged and
carry no Convert button (matches `!so.project_id`-style "already resolved" gating elsewhere in the
app). Full click-through of every flow (actually converting a lead, inspecting the pending return,
issuing the draft invoice) was **not** exercised live this pass — the data-layer verification (4
repeated script runs, stable counts, 0 FK violations, counter safety) was judged sufficient; do a
UI click-through before an actual live demo if time allows.

---

## 2026-08-22 — Report Engine data fix + additive Procurement seed

**Bug found while verifying REPORT-ENGINE-PLAN.md's reports, fixed in the live DB.** The two
`sales_invoices` rows this script's own seed inserts (`SB/13/2026-27`, `SB/14/2026-27`) left
`cgst_amount`/`sgst_amount`/`igst_amount` at their schema default (`0`) while `tax_amount`/`total`
were set explicitly — both customers (Asian Brown, GSTIN state 24; Himalayan Dairy, state 05) are
interstate from the issuing company (Telangana, 36), so the correct split is IGST-only, matching
what `gstSplit()` (`lib/gst-calc.mjs`) computes for every invoice made through the real
`convert-to-invoice` flow. Fixed: `scripts/seed-sales-marketing-demo.mjs` now sets `igst_amount`
explicitly on both inserts (re-runs will be correct from now on); the two already-seeded live rows
were corrected directly (`igst_amount = tax_amount`) via a one-off script, run once and deleted.

**New file: `scripts/seed-report-demo-extra.mjs`.** Run: `node --env-file=.env.local
scripts/seed-report-demo-extra.mjs`. Purely additive (marked `created_by='report-demo-seed'`,
re-runnable, deletes only its own rows first) — the live DB had exactly **1** `purchase_orders` row
and 2 `vendor_bills` before this, too thin to demo Purchase Register/Vendor Ledger/AP Aging with
more than one supplier. Adds 2 POs (a 3rd/4th supplier: A R Engineering Solution, 3D Foamcut) each
with one issued Vendor Bill — one paid in full (with its Vendor Payment), one left open — so AP
Aging has a real open item beyond the pre-existing A P Motronix bill, and Purchase Register/Vendor
Ledger have more than one counterparty to show. **Does not touch**
`scripts/seed-procurement-demo.mjs`'s scope or run it — that script is documented retired/stale
above and does an unconditional `DELETE FROM purchase_orders` with no `created_by` guard, which
would also destroy this and any other chat's live procurement data.

---

## Full classification (every table, live counts, queried directly 2026-08-17, Stores rows updated 2026-08-19)

| Table | Rows | Class | Notes |
|---|---:|---|---|
| **suppliers** | **445** | 🟢 PRODUCTION | Real STERP vendor master. Unchanged by the rebuild. **Do not fabricate over.** |
| employees | 21 | 🟢 PRODUCTION-ish master | HR roster, unchanged by the rebuild. |
| operations / workstations / trades | 9 / 7 / 8 | 🟢 MASTER | Production's small pickers, unchanged. |
| customers | 3 | 🟡 ACCUMULATING MASTER | Reclassified this revision — see note above. All 3 are from the 2026-08-17 rebuild (Ganga Textiles, Konkan Sugars, Asian Brown Bleachchem). |
| items | 2,773 | 🟢 REAL MASTER, LOADED + WIRED (2026-08-17) | Loaded via `scripts/import-item-master.mjs`. Wired into BOM/inventory creation same day (STORES-SALES-CHANGES.md §3.2, done) — via `items.id`, not `item_code` (only 1 of 2,773 rows actually has one populated, confirmed against the real export). `bom_items.item_id`/`inventory_items.item_id` set when a line is picked from the catalog search (PrWorkspace's ItemSearchField, Stores' New Item dialog) or auto-linked on an exact name match during PMB import; no retroactive backfill of pre-existing free-text rows. |
| projects | 3 | 🟡 DEMO | SB-1022 (Konkan Sugars, id 13), SB-1018 (Asian Brown, id 14, rebuilt in place — same project_no as before), plus the `is_system=1` sentinel row (id 15, `—NON-PROJECT—`, not a real project — backs Stores' stock/SAS flow). |
| sale_orders / sale_order_items | 3 / 4 | 🟡 DEMO | SO-6 (Ganga Textiles — **no project**, stops at Sale Order by design), SO-7 (Konkan Sugars), SO-8 (Asian Brown). |
| quotations / quotation_items | 3 / 4 | 🟡 DEMO | One per Sale Order above, all `status='accepted'`. |
| leads | 3 | 🟡 DEMO | One per customer, `status='converted'`. |
| opportunities / opportunity_items | 5 / 0 | 🟡 DEMO (untouched) | Pre-existing from `seedV3DemoData` (app boot seed), not part of the rebuild — the new script never creates or deletes opportunities. |
| scope_of_supply | 2 | 🟡 DEMO | One per project (Ganga Textiles has none — no project). |
| milestones | 50 | 🟡 DEMO | 25 × 2 real projects. |
| milestone_stages | 0 | ⚪ EMPTY — **regression, see Gaps** | Was 3 before the rebuild. |
| calc_sheets / calc_variables | 2 / 20 | 🟡 DEMO | One sheet per project; the sheet's inputs render from library defaults, not seeded rows (see Gaps). |
| calc_drawings / calc_drawing_files / calc_drawing_comments | 5 / 0 / 4 | 🟡 DEMO | 3 on Konkan Sugars (one deliberately left `under_review` + unshared, one live-interactive, one pre-approved with a thread), 2 on Asian Brown (both approved). No files uploaded (demo has no real drawing PDFs). |
| bom_items | 9 | 🟡 DEMO | 4 (Konkan, mixed PENDING/TRANSIT/RECEIVED) + 5 (Asian Brown, mostly CLOSED/RECEIVED). |
| bom_imports | 0 | ⚪ EMPTY | Demo BOM seeded directly, not via import — unchanged. |
| job_cards / job_card_time_logs / job_card_consumables | 2 / 0 / 0 | 🟡 DEMO | Both on Asian Brown (Shell Welding, Painting — both `done`). Konkan Sugars has none (too early-stage for shop floor). |
| qc_documents / qc_document_parts | 1 / 2 | 🟡 DEMO — **much thinner than before, see Gaps** | Was 4 / 216 before the rebuild. |
| test_certificates | 2 | 🟡 DEMO — **much thinner than before, see Gaps** | Was 17 (real-derived from the client's SB-1037 sample) before the rebuild. |
| qc_records / qc_mountings | 0 / 0 | ⚪ EMPTY | Unchanged. |
| packing_lists / packing_items | 1 / 5 | 🟡 DEMO — **thinner than before, see Gaps** | Was 2 / 137 before the rebuild. One `ready` list on Asian Brown. |
| procurement_requests / supplier_quotes / purchase_orders / po_items | 0 / 0 / 0 / 0 | ⚪ EMPTY — **regression, see Gaps** | Was 8 / 24 / 2 / 8 before the rebuild (from `scripts/seed-procurement-demo.mjs`, now stale — see Gaps). |
| tasks | 0 | ⚪ EMPTY | Was 16 (Incidents demo) — wiped, not rebuilt (out of scope for this rebuild). |
| notifications | 5 | 🟡 DEMO/runtime | Includes the customer-drawing-shared and comment/approval notifications generated while browser-verifying this session's new feature. |
| rfqs / rfq_items / rfq_suppliers | 0 / 0 / 0 | ⚪ EMPTY | Unchanged. |
| pr_items / pr_item_projects / purchase_requisitions | 0 / 0 / 0 | ⚪ EMPTY | Unchanged. |
| inventory_items / inventory_reservations | 3 / 0 | 🟡 DEMO — **updated 2026-08-19** | MS Angle 50x50x5 (8/10, below minimum — the Reorder Suggestions demo row), MS Plate 10mm (50/10), MS Plate 10mm (remnant demo, piece-tracked). Reservations are 0 — no seed script commits a reservation, only `seed-demo-pipeline.mjs`'s own wipe touches this table (see its own DELETE list). |
| gate_inward_receipts | 2 | 🟡 DEMO — **new 2026-08-19** | Seeded by `scripts/seed-stores-gate-demo.mjs`: 1 open (awaiting GRN), 1 closed (with a GRN ref) — see "2026-08-19 Stores gate-feature seed" below. |
| gate_passes / gate_pass_items | 6 / 6 | 🟡 DEMO — **new 2026-08-19** | Same script. One of every status the UI exercises: draft, approved, issued-not-due, issued-**overdue**, returned, and non-returnable-issued. |
| attendance_days / crm_notes / campaigns | 0 / 0 / 0 | ⚪ EMPTY | Unchanged. |
| stage_templates / stage_template_items | 0 / 0 | ⚪ EMPTY | Unchanged. |
| counters | 7 | 🟢 SEQUENCES | Live business sequences (`po_no`, `project_no`, `quotation_no`, `sale_order_no`, etc.) — never reset. |
| app_settings | 1 | 🟢 CONFIG — **new 2026-08-20** | One global row: `stores_allocation_mode` (auto/manual), backing Stores' real Allocation Mode toggle. Same never-reset treatment as `counters`. |
| tickets | 1 | ⚫ DEAD | Pre-collapse history, kept for FK only. Nothing reads it. |
| users | 22 | 🟡 DEMO logins + ⚠️ 2 stray | See below — unresolved from the 2026-08-04 snapshot. |
| machines / usb_devices / usb_requests / usb_audit | 2 / 1 / 6 / 623 | 🟡 DEMO/runtime | Security platform demo + audit trail, grown from browser-verification activity across sessions. |
| approval_policies / browser_requests | 3 / 0 | 🟡 DEMO / ⚪ EMPTY | Unchanged. |

**⚠️ Stray users, still unresolved from 2026-08-04:** `TEST-USER` (role: operator) and
`productionshantiteam` (role: operator) are still in the `users` table, not part of the documented
demo login set. Same recommendation as before — decide whether to keep, rename, or remove before a
real demo, since they'd appear in login/roster lists.

---

## 2026-08-23 — demo-readiness pass: department-stage map + portal logins for every demo customer

Driven by a direct ask: a live-call reference (delivered separately, not duplicated here — see
SYSTEM.md §5am) needed a real "jump straight to this department's stage" table, and every demo
customer needed a working portal login. Both are now real, current as of this date.

**Department-stage map** (queried live, not estimated) — which project to open to demo a specific
department mid-flow, instead of building one from scratch on a call:

| Project | Customer | BOM lines | PO | Job cards | QC records | Packing lists |
|---|---|---|---|---|---|---|
| SB-1018 (id 17) | Asian Brown Bleachchem | 22 | 0 | 2 | 3 | 2 (1 ready, 1 dispatched w/ freight+invoice+e-way) |
| SB-1023 (id 16) | Konkan Sugars | 5 | 0 | 0 | 0 | 0 |
| SB-1024 (id 18) | Vindhya Paper Mills | 6 | 0 | 0 | 0 | 0 |
| SB-1025 (id 19) | Deccan Sugar Works | 2 | 1 | 0 | 0 | 0 |
| SB-1026 (id 20) | Coromandel Textiles | 2 | 0 | 0 | 0 | 0 |
| SB-1027 (id 21) | Malwa Steel Industries | 2 | 0 | 1 | 0 | 0 |
| SB-1028 (id 22) | Godavari Agro Processors | 1 | 0 | 0 | 1 | 0 |
| SB-1029 (id 23) | Narmada Chemicals | 3 | 0 | 1 | 0 | 1 (ready) |
| SB-1030 (id 24) | Kaveri Spinning Mills | 3 | 0 | 0 | 1 | 0 |
| SB-1031/1032/1039 | Himalayan Dairy / Ganga Textiles / Bharat Sugar | 0 | 0 | 0 | 0 | 0 | empty shells, not useful demo examples |

SB-1018 remains the only project with a shared QC certificate and an issued invoice — it's the one
to use for the Customer Portal's full document set (§6). Everything else here is a real project at
a real, distinct pipeline stage, not fabricated for the table.

**Customer Portal logins — every project-linked demo customer now has one**
(`scripts/seed-customer-portal-demo.mjs`, idempotent). All 12 rows in `customers` use `.example`
addresses (RFC 2606, non-routable) — this table has never held real business data, confirmed by
querying every row's email domain, not assumed. Password convention matches the original 3 demo
accounts (`<username-slug>123`). `customers.portal_enabled` deliberately left `0` for all of
them — that flag only gates whether email is *attempted*; login access needs nothing but the
`users` row + `project_ids`, already created. Nothing has ever been emailed to any of these.

| Customer | Portal username | Project |
|---|---|---|
| Asian Brown Bleachchem | `asian_brown` | SB-1018 |
| Konkan Sugars Ltd | `konkan_sugars_ltd` | SB-1023 |
| Vindhya Paper Mills Ltd | `vindhya_paper_mills_ltd` | SB-1024 |
| Deccan Sugar Works | `deccan_sugar_works` | SB-1025 |
| Coromandel Textiles Pvt Ltd | `coromandel_textiles_pvt_ltd` | SB-1026 |
| Malwa Steel Industries | `malwa_steel_industries` | SB-1027 |
| Godavari Agro Processors | `godavari_agro_processors` | SB-1028 |
| Narmada Chemicals Ltd | `narmada_chemicals_ltd` | SB-1029 |
| Kaveri Spinning Mills | `kaveri_spinning_mills` | SB-1030 |
| Himalayan Dairy Pvt Ltd | `himalayan_dairy_pvt_ltd` | SB-1031 (empty shell) |
| Ganga Textiles Pvt Ltd | *(no email on file — nothing to link)* | SB-1032 (empty shell) |
| Bharat Sugar Mills Ltd | *(no email on file — nothing to link)* | SB-1039 (empty shell) |

**Fixed 2026-08-23 (SYSTEM.md §5ao, plan §2):** SB-1032's and SB-1039's `projects.customer_id` were
both NULL, matched only by free-text `customer_name` — backfilled to the real customer id (Ganga
Textiles → id 2, Bharat Sugar Mills → id 14) now that the match was confirmed, so both rows above no
longer say "not seeded — no `customer_id`." `hkm_charitable` and `virchow_biotech` (the two other
original demo logins, pre-dating the CRM `customers` table) were genuinely broken —
`hkm_charitable`'s `project_ids` resolved to an unrelated customer's project (SB-1023, Konkan
Sugars), `virchow_biotech`'s resolved to no project at all — deactivated (`users.active = 0`) rather
than fabricating a project for either, since there's no real project to point them at.

---

## Gaps introduced or left open by the 2026-08-17 rebuild

Reporting these rather than fixing them — each is real, scoped work, not something to silently patch
in passing:

1. **`scripts/seed-procurement-demo.mjs` is now stale and will error if run.** It hardcodes
   `PROJECTS = [3, 4, 5, 6]` — the old project ids, all deleted by the rebuild. Running it today
   would either no-op (`WHERE project_id IN (3,4,5,6)` deletes on the wipe step, matches nothing) or
   fail outright depending on the exact statement. **Effect: Procurement's Sourcing, Selection, and
   Purchase Orders tabs currently have nothing to show** — 0 quotes, 0 POs, 0 comparison data,
   whereas before the rebuild there were 24 quotes and 2 multi-project POs specifically built to
   demo that flow. Fixing this means either pointing that script at the new project ids (13, 14) or
   rewriting it to look projects up by `project_no` instead of a hardcoded array — real work, not
   attempted here.
2. **QC's Certificate bank and statutory document demo shrank sharply.** Before: 17 test certificates
   (real-derived from the client's actual SB-1037 sample) and a 216-part Form IV A document. Now: 2
   certificates and a 2-part document, built fresh for this rebuild's narrower "near-complete
   project" story. QC's own workspace will look much thinner in a demo than it did two weeks ago.
3. **Dispatch's packing demo shrank.** Before: 2 packing lists, 137 items. Now: 1 list, 5 items.
   **Superseded as of 2026-08-23** — see the "Department-stage map" section below; Dispatch now has
   3 packing lists across 2 projects, one fully dispatched with freight/invoice/e-way populated.
4. **Workflow Stages (`milestone_stages`) has zero rows.** The rebuild's milestone-seeding is a
   simplified, explicit version (see the script's own comment) that does not call
   `copyDefaultStageTemplate`, unlike the app's own `createProjectMilestones`. Any milestone on
   either of the 2 rebuilt projects will show an empty Stages checklist even where the real app would
   normally auto-copy a default template.
5. **Calc Sheets have no seeded `calc_variables` rows for the 2 new projects**, unlike the original
   app-seeded SB-1018 sheet. The Calculation screen still renders (it falls back to library defaults
   when a sheet has no rows of its own), so this isn't visibly broken, but a snapshot/history story on
   these sheets would be thin if someone tried to demo that specifically.
6. **`tasks` (Incidents demo, Outgoing/Incoming) is empty**, was 16 rows before. Out of scope for
   this rebuild (it targeted the sales-through-dispatch pipeline story, not Operations' incident
   cards), not rebuilt.

None of these are urgent — the rebuild's actual goal (a clean, distinct-stage Lead→Sale
Order→Project→Design→BOM→QC→Job Cards→Packing walkthrough, plus the new customer drawing-approval
feature) is intact and browser-verified end to end. These are the corners that got thinner as a
side effect, worth knowing about before anyone demos Procurement, QC's certificate bank, Dispatch,
or Workflow Stages specifically.

---

## 2026-08-24 — Reports-polish seed scripts (backfilled, never logged here) + QC HEADERS demo data

**Backfill note first:** the 4 seed scripts below were built and run in an earlier session the same
day (Excel export + QC report gaps work) but never got an entry in this doc — recording them now so
this inventory stays the actual source of truth, not just the newest work.

- **`scripts/seed-qc-inspections-demo.mjs`** — additive, tagged `created_by='qc-inspections-demo-seed'`.
  `calibration_items` and `job_work_inspections` were both at **0 rows** DB-wide before this (not just
  thin — the two new QC reports built the same session had nothing to show at all). Adds ~10
  calibration items spanning all 4 derived statuses (expired/due_soon/ok/blocked), ~8 job-work
  inspections across existing Shanti Boilers projects, and ~10 more `qc_records` (new test_types,
  a fail/pending mix) so Inspection Pass/Fail Summary isn't monotone.
- **`scripts/seed-production-demo.mjs`** — additive, tagged `created_by='production-demo-seed'`.
  `job_card_time_logs` was at 0 rows DB-wide, and **every** employee/workstation had a NULL cost
  rate — Labour Utilization and Production Cost Variance's planned-labour figure were structurally
  zero regardless of how much time-log data got seeded. Also fills those NULL
  `cost_rate_per_hour`/`machine_hour_rate` columns on a few existing Production employees/
  workstations directly (idempotent UPDATE, not a demo-row insert — this is baseline data that was
  simply never set). Adds 4 work orders (one of each status incl. a deliberately-late one), 6 job
  cards (2 with real rejections), ~16 time logs, 5 material issues, 2 full cut events.
- **`scripts/seed-design-dispatch-procurement-demo.mjs`** — additive, one file/three independent
  scopes, confirmed thin by a live row-count pass (not assumed): Design's `bom_change_notes` (1 row),
  Dispatch's `packing_lists` (3 rows total), Procurement's `purchase_orders` (3 rows). Adds ~8 ECNs,
  ~6 packing lists (mixed dispatched/ready/draft), ~6 POs (2 with real aging) — and flips 3 existing
  `bom_items.purchase_status` from `Enquiry` to `Transit`, needed for Open PO Aging (see the bug note
  below) to have anything to show.
- **`scripts/seed-sales-marketing-demo.mjs` extended** (same `MARK`, no new file) — 5 more
  `sales_invoices` across new customers; Sales Register reads invoices, not `sale_orders` (already
  reasonably populated), and only had 2 distinct customers before this.

**Real bug found while seeding, fixed in `lib/data.js`:** `getOpenPoAgingLines` compared
`bom_items.purchase_status` against the literal `'TRANSIT'` (uppercase) when the actual stored value
used everywhere else in the codebase is `'Transit'`. SQLite string comparison is case-sensitive, so
Open PO Aging was silently empty regardless of real Transit-status data — not a seed-data gap, a real
query bug, now fixed to match the canonical value.

**Today's QC/HEADERS work — `scripts/seed-qc-headers-demo.mjs`.** Two things in one additive,
re-runnable script:
1. **`projects.series` backfill** on 3 existing demo projects (`SB-1018`→SF, `SB-1023`→CF,
   `SB-1024`→PRS) — idempotent `UPDATE ... WHERE series IS NULL`, not a demo-row insert. Root cause:
   **every single project in this DB had `series = NULL`** before this — the QC workspace's Model
   filter was never actually broken, it just had nothing real to filter (see SYSTEM.md for the full
   investigation: the existing backfill script `scripts/qc-reassign-certs.mjs` infers the model from
   a structured `project_no` format this system's real `SB-NNNN` auto-numbering never produces, so
   re-running it would match zero projects).
2. **One new project (`SB-DEMO-SH-1100`, `series='HEADERS'`, `company='Shanti Boilers'`)** + a real
   `qc_documents` row (Form III + Form III-H) seeded from **the client's own uploaded sample**
   (`FORMIIIIVA_1100_3H.xlsx`, real maker's no `SB-IBR-SH-1100A`, real customer "Buildmate Projects
   Pvt Ltd", real 13-part list with real melt numbers) — not fabricated data. 5 `test_certificates`
   + `certificate_projects` links + 4 `qc_mountings` rows, also copied from the client's real samples.
   One deliberate demo-only deviation, commented in the script: part 13 ("Branch Pipes") is linked to
   a certificate for the Preview-PDF flow to work end-to-end, even though the real sample leaves that
   row's certificate reference blank.

Re-run: `node --env-file=.env.local scripts/seed-qc-headers-demo.mjs` (and the 3 scripts above, any
order — none share scope with each other or with `seed-demo-pipeline.mjs`).

---

## Wipe gotcha (ordering constraint for any demo seed)

`POST /api/masters/suppliers/import` does a **full replace** and clears `purchase_orders` +
`supplier_quotes` first (FK-safe, client-accepted). So:
- **If you re-run the supplier import live during a demo, it deletes any fake quotes/POs.**
- **Correct order:** load/settle suppliers **first**, then create fake procurement activity **on
  top**.
- The same principle now applies to `scripts/seed-demo-pipeline.mjs`: it never touches `suppliers`,
  `customers`, `employees`, or any other master, by design — same precedent as the retired
  `seed-procurement-demo.mjs` script it partly supersedes.

---

## Current demo-data source of truth

**`scripts/seed-demo-pipeline.mjs`** (2026-08-17) is now the canonical demo-data script for the
sales-through-dispatch pipeline. Re-runnable: it wipes its own scope first (see the script's own
comment for the exact table list and delete order) and rebuilds the same 3-stage story every time.

**`scripts/seed-stores-gate-demo.mjs`** (2026-08-19) is the canonical demo-data script for Stores'
gate features (GIR, Gate Pass) — see the dated section above. Independent scope from the pipeline
script above; run either in any order, neither touches the other's tables.
Run: `node --env-file=.env.local scripts/seed-demo-pipeline.mjs`.

`scripts/seed-procurement-demo.mjs` (2026-08-04) is **retired/stale** as of this rebuild (see Gap 1
above) — kept in the repo for reference, not safe to run against the current DB without fixing its
hardcoded project ids first.

**Reports-polish + QC HEADERS scripts (2026-08-24, see the dated section above for full detail)** —
all independent scope from every script above and from each other, run in any order:
- `scripts/seed-qc-inspections-demo.mjs`
- `scripts/seed-production-demo.mjs`
- `scripts/seed-design-dispatch-procurement-demo.mjs`
- `scripts/seed-qc-headers-demo.mjs`
- `scripts/seed-sales-marketing-demo.mjs` was extended in place (same file, same `MARK`), not
  superseded.

---

## Reconciliation with D18 (the "wipe + minimal single-project reseed" decision)

D18 (V2-CHANGES.md) said: for eventual clean-up, wipe everything and reseed one minimal synthetic
project. That was about a *future clean baseline* once real masters arrive. **Still not the current
approach** — the 2026-08-17 rebuild deliberately went the other way (3 projects, each at a distinct
stage, richer than one minimal project) for demo/walkthrough purposes, same reasoning the original
2026-08-04 plan gave for preferring a multi-project demo over D18's minimal one. Keep D18 as the
eventual production-cleanup path once `customers`/`items` masters are fully real-loaded; use this
doc's current snapshot for the near-term demo state.
